# Implementation Plans

Generated by the improve skill on 2026-06-11, against commit `be54731`.
Execute in the order below unless dependencies say otherwise. Each executor:
read the plan fully before starting, honor its STOP conditions, and update
your row when done.

Repo verification gates (all plans): `pnpm typecheck` && `pnpm test` &&
`pnpm build` — all green at planning time (21 tests).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Remove credential-bearing scratch file (`mcp.tests.txt`) | P1 | S | — | TODO |
| 002 | Test coverage for the CLI surface (helpers, snapshot, JSON, e2e) | P1 | M | — | TODO |
| 003 | Fix Ajv `$id`-collision false positives | P1 | S | — (nicer after 002) | TODO |
| 004 | Follow `tools/list` pagination cursors | P1 | S | 002 (stub pattern) | TODO |
| 005 | Housekeeping: README sync, single-source version, drop zod + stale scripts | P2 | S | — | TODO |
| 006 | Upgrade vitest to v4 (clears critical audit advisory) | P3 | S–M | 002, 003, 004 | TODO |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) |
REJECTED (with one-line rationale).

## Dependency notes

- **001 is independent and urgent** — and the token rotation it mandates can
  only be done by the maintainer (PostHog account access), not an executor.
- **004 extends `test/fetch.test.ts` created by 002.** If run before 002, the
  plan documents how to self-create the stub.
- **006 runs last** so the test-runner major bump is validated against the
  full suite (002–004's new tests), not just the original 21.
- 002 and 005 interact mildly: 005 removes the direct `zod` dependency; 002's
  e2e fixture documents a fallback that doesn't rely on it. Either order
  works.

## Findings considered and rejected

- `normalizeTool` blind-casts schemas (`src/client/fetch.ts:33-35`): boolean/
  non-object JSON Schemas degrade gracefully (odd message text only); not
  worth its own plan. Revisit if user reports surface.
- Heuristic gating asymmetry (`src/linter/heuristic.ts:32` vs `:42`):
  readonly check gates only on `readOnlyHint`, mutating check on any
  annotation — reads as intentional conservatism; info-severity only.
- `parseStdioCommand` minimal quoting (`src/client/connect.ts:44-59`):
  explicitly documented as intentional v1 scope.
- `commander` 12→15, `typescript` 5→6, `zod` 3→4 majors: no security driver;
  deferred (zod is instead *removed* by plan 005).
- Release workflow safety: `prepublishOnly` already runs
  typecheck + test + build before publish — no gap.

## Direction options (not planned — maintainer's call)

Surfaced during the audit; say the word and any of these becomes a
design/spike plan:

- **D1 — Layer 2 spike**: README (`:14-22`) promises CSP checks, ext-apps
  conformance, `structuredContent` validation; none exists yet.
- **D2 — Lint prompts & resources**: only tools are linted; the snapshot
  model was built to extend. Reuse plan 004's cursor loop.
- **D3 — Rule configuration** (`--rule id=off` / `.mcplintrc`): CI adopters
  need to silence noisy rules (e.g. `output-schema-missing`).
- **D4 — SARIF output**: gets findings into GitHub code scanning for free.

## Audit coverage note

Full direct read of `src/` and `test/` (~1k lines) at `be54731`; dependency
audit via `pnpm audit`/`pnpm outdated`; CI workflows read. Not audited:
`.agents/` tooling content (config, not source) and `dist/` (generated).
